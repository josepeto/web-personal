from django.apps import AppConfig


class PortfolioConfig(AppConfig):
    name = 'portfolio'
    #La sig. instrucción nos permitirá entre otros, sustituir el contenido del campo anterior por el siguiente, más descriptivo.
    verbose_name ='Portafolio'